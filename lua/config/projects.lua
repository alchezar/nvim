-- Local list of pinned projects for Startify dashboard.
-- This file is tracked but treated as local via skip-worktree.
--
-- Each entry: either a plain path string (uses basename as label), or
--   { name = 'Display name', path = '~/...' }
return {
  { name = 'NeoVim', path = '~/.config/nvim' },
}
